sudo: required

services:
  - docker

before_install:
  - gem install bundler
  - docker build -t prahalladkb/docker-react -f Dockerfile.dev .

script:
  - docker run prahalladkb/docker-react npm run test -- --coverage
